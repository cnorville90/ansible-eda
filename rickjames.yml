---
- name: The Rick James Test Playbook!
  hosts: all
  gather_facts: false
  vars:
    ocp_dep: foo
    ocp_ns: bar

  tasks:

    - name: Print something cool
      ansible.builtin.debug:
        msg: "I'm Rick James!! running on {{ inventory_hostname }}! Enjoy Yourself!"
